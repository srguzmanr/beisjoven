---
// Structural reservation for the first sponsor slot. When SPONSOR_ACTIVE flips
// to true (Sprint 5), the component renders the real sponsor inside an
// <aside aria-label="Patrocinado"> wrapper without re-layout. Until then it
// renders nothing and collapses to zero height.
const SPONSOR_ACTIVE = false;
---

{SPONSOR_ACTIVE && (
  <aside aria-label="Patrocinado">
    {/* TODO Sprint 5: render sponsor creative (970×90 desktop / 320×50 mobile) */}
  </aside>
)}
